export const PAGESIZE = 40
export const INITIAL_PAGE = 1
export const LIMIT_ITEMS = 1000
export enum SEARCH_KEY {
  SORTED_BY = "sortedBy",
  SEARCH = "search",
  HASH_TAG = "hashTag",
  CURRENT_HASH_TAG = "currentHashtag",
}

export enum OPERATE_TYPE {
  PIN = "pin",
  FORK = "fork",
  RUN = "run",
  CREATE = "create",
}
